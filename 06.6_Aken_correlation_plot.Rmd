---
title: "Untitled"
output: html_document
date: "2025-08-05"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
hist_plot + 
lin_af_plot + 
parent_af + 
pca2 +
plot_annotation(tag_levels = "A") + plot_layout(nrow = 2, guides = "collect")

a<-hist_plot + 
lin_af_plot + 
  plot_annotation(tag_levels = "A") + plot_layout(ncol = 2, guides = "collect", widths = c(2,1))

b<-parent_af + 
pca2 +
plot_annotation(tag_levels = "A") + plot_layout(ncol = 2, guides = "collect")

c<- a / b + plot_annotation(tag_levels = "A") + plot_layout(nrow = 2, guides = "collect")
c

##
a<-hist_plot +
  plot_annotation(tag_levels = "A") + plot_layout(ncol = 1, guides = "collect")

b<- lin_af_plot +
parent_af + 
pca2 +
plot_annotation(tag_levels = "A") + plot_layout(ncol = 3, guides = "collect")

c<- a / b + plot_annotation(tag_levels = "A") + plot_layout(nrow = 2, guides = "collect")
c
```

